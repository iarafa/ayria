# AYRIA - Dependências do Backend
# Pin versões principais pra reprodutibilidade

# Core
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.3
asyncpg==0.30.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validação
pydantic==2.9.2
pydantic-settings==2.6.0
email-validator==2.2.0

# HTTP client (p/ IA + Azure)
httpx==0.27.2
openai==1.55.0  # também serve pra Minimax (OpenAI-compatível)

# Vetor (Qdrant)
qdrant-client==1.12.0

# Storage Azure
azure-storage-blob==12.23.0

# Utilitários
python-dotenv==1.0.1
loguru==0.7.2
tenacity==9.0.0  # retry
langchain==0.3.6
langchain-text-splitters==0.3.2  # chunking

# Testes
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
